name: flutter_application_romeo
description: A new Flutter project for Romeo Energy Community.
publish_to: 'none' # Prevents accidental publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.2.0
  firebase_auth_web: ^5.11.0
  firebase_database: ^11.1.4  # ← AGGIUNTO per Realtime Database

  # Web interop
  js: ^0.7.1

  # UI & Icons
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  path_drawing: ^1.0.1

  # Charts
  fl_chart: ^0.69.0
  syncfusion_flutter_charts: ^28.1.38

  # Localization
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/diagrams/cer_animata.svg
