{
  "name": "romeo-functions",
  "type": "module",
  "engines": {
    "node": "^20"
  },
  "scripts": {
    "build": "echo 'no build'",
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions,hosting",
    "deploy": "firebase deploy --only functions,hosting"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.11.1"
  }
}